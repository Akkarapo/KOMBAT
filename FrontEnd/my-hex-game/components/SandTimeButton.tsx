"use client";

import React from "react";

interface SandTimeButtonProps {
  onRecalculate: () => void;
}

const SandTimeButton: React.FC<SandTimeButtonProps> = ({ onRecalculate }) => {
  return null; // Removed button rendering as it was already present in the original code.
};

export default SandTimeButton;
